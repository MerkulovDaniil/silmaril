name: Keep alive + reset playground

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sf -X POST ${{ secrets.RENDER_URL }}/api/reset > /dev/null
